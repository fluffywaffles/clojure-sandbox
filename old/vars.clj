(ns topology.vars
  (:require [util.macros :refer [declare-dyn]]))

(declare-dyn max-pxcor
             min-pxcor
             max-pycor
             min-pycor
             wrap-in-x?
             wrap-in-y?)
